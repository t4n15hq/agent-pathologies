# Recommended: one key, all models (Qwen / DeepSeek / GLM / Claude / GPT).
OPENROUTER_API_KEY=

# Direct providers (only needed if you bypass OpenRouter).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
TOGETHER_API_KEY=
FIREWORKS_API_KEY=

# Local vLLM / Ollama need no key.
